services:
  relay:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "8080"
    environment:
      ADMIN_TOKEN: ${ADMIN_TOKEN:-}
    volumes:
      - outbox-data:/data
    restart: unless-stopped

volumes:
  outbox-data:
