{
  "name": "outbox-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Outbox MCP client + CLI for cross-machine AI agent messaging",
  "type": "module",
  "bin": {
    "outbox-mcp": "./dist/index.js",
    "outbox": "./dist/cli.js"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc && node scripts/chmod-bins.mjs",
    "watch": "tsc -w",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "commander": "^12.1.0",
    "eventsource": "^3.0.2",
    "node-notifier": "^10.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/node-notifier": "^8.0.5",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
